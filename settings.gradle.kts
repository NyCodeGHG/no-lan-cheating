rootProject.name = "no-lan-cheating"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://maven.fabricmc.net/")
        maven("https://maven.parchmentmc.org/")
    }
}
